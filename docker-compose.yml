#version: '3.8'
services:
  parser:
    build: .
    container_name: apple-health-parser
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
    command: python parse_apple_health.py /app/data/export.xml

  # Placeholder for database - we'll add this later
  # db:
  #   image: postgres:15
  #   container_name: apple-health-db
  #   environment:
  #     POSTGRES_DB: apple_health
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   postgres_data: